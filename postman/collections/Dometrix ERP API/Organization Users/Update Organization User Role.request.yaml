$kind: http-request
name: Update Organization User Role
method: PUT
url: "{{base_url}}/api/v1/organization-users/{{organization_user_id}}"
order: 4000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "role_id": "{{role_id}}"
    }
