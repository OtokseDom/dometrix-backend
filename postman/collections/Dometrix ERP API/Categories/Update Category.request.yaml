$kind: http-request
name: Update Category
method: PATCH
url: "{{base_url}}/api/v1/categories/{{resource_id}}"
order: 4000
headers:
    Content-Type: application/json
    Accept: application/json
body:
    type: json
    content: |-
        {
          "name": "Raw Materials - Premium",
          "metadata": {
            "description": "All raw materials used in manufacturing - updated",
            "updated_date": "2026-04-01"
          }
        }
